[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "auth-api"
version = "1.0.0"
description = "Authorization API"
authors = [
    {name = "Developer", email = "dev@example.com"}
]
dependencies = []  # 这里留空，没有外部依赖
